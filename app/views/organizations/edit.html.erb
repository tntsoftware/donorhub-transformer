<div class="container">
  <div class="row">
    <div class="col-md-4 offset-md-4">
      <%= bootstrap_form_for(@organization) do |f| %>
        <div class="mb-3">
          <%= f.text_field :name, label: 'Workspace Name' %>
        </div>
        <%= f.submit "Save", class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>